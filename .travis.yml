language: node_js
node_js:
  - "8"
services: mongodb  
sudo: false
install:
  - npm install
script:
  - echo "Doing the test." && npm test
deploy:
  - provider: script
    script: chmod +x ./deploy.sh && ./deploy.sh
    on:
      branch: master
